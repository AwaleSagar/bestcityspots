# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/
*.pem
*.key
*.crt
*.p12
*.pfx

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
lerna-debug.log*
*.log
logs/

# editors / IDEs
.idea/
*.swp
*.swo
*~
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
!.vscode/launch.json
!.vscode/mcp.json

# env files — only the documented template is committed
.env*
!.env.example

# vercel
.vercel

# typescript / build caches
*.tsbuildinfo
next-env.d.ts
.turbo/
.eslintcache
.cache/
.parcel-cache/

# python / local tooling
.venv/
__pycache__/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.cursor/

# backups
/my-backup
/my-backup.zip

# agents/AI
.agent
.agent/*
.agents/*

# downloaded reference data (GeoNames, World Bank) — reproducible via npm run db:seed
/data/

# supabase local dev
supabase/.branches/
supabase/.temp/
supabase/.env

# misc tooling
*.tgz
.npm/
.firecrawl/

# Internal UI/UX review reference (not tracked)
bestcityspots.md

# Internal backend incident reference (not tracked)
bestcityspots-backend-incident-actions.md

# Internal user-story planning artifact (not tracked)
bestcityspots-user-stories.md

# Internal color audit / brainstorm (not tracked)
bestcityspots-color-audit.md

# Internal visual-asset generation prompts / creative production reference (not tracked)
bestcityspots-visual-asset-prompts.md

# Ansible: never commit real inventory or decrypted secrets (examples are fine)
deploy/ansible/inventory.ini
deploy/ansible/group_vars/vault.yml
public/_incoming/
